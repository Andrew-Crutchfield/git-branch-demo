// import express from 'express';
// import chirpsRouter from './routes/chirps'; // Replace with the correct relative path

// const app = express();
// const port = process.env.PORT || 3000;

// app.use(express.json()); // Middleware to parse JSON bodies

// app.use('/chirps', chirpsRouter); // Use the chirps router on the '/chirps' route

// app.get('/', (req, res) => {
//     res.send('Welcome to the Chirper API!');
// });

// // Start the server
// app.listen(port, () => {
//     console.log(`Server listening on port ${port}`);
// });